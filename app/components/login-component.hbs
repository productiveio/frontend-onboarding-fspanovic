<div class="login-container">
  <form class="login-form" {{on "submit" this.submitLogin}}>
    <div class="form-group">
      <label for="email">Email</label>
      <input
        class="form-input"
        id="email"
        type="text"
        value={{this.email}}
        required
        {{on "input" this.onInputChange}}
      />
    </div>
    <div class="form-group">
      <label for="password">Password</label>
      <input
        class="form-input"
        id="password"
        type="password"
        required
        value={{this.password}}
        {{on "input" this.onInputChange }}
      />
    </div>
    <button class="form-button" type="submit" >Login</button>
  </form>
</div>